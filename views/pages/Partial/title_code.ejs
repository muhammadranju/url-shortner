<h1 class="mt-4 text-sky-500 font-bold text-center text-4xl">URL Shortener</h1>
<div class="card mt-4 rounded-2xl bg-slate-800">
  <div class="card-body">
    <h5 class="card-title text-center mb-4 text-lg font-medium">
      Paste the long URL and make it short!
      <a href="/"
        ><button class="btn bg-sky-500 rounded-full">Reload</button></a
      >
    </h5>
    <form id="shortForm" onsubmit="return false" action="/" method="post">
      <div class="input-group mb-4">
        <input
          type="text"
          class="form-control border-0 focus:text-slate-100 form-control-lg focus:bg-slate-900 text-slate-200 bg-slate-900 rounded-full"
          name="longURL"
          id="longURL"
          required
          placeholder="https://example.com"
        />
        <div class="input-group-append">
          <button class="btn bg-sky-500 rounded-full" id="create">
            SHORTEN
          </button>
        </div>
      </div>
    </form>
    <p class="mt-4 text-center text-lg font-semibold">
      This is a free online service which helps you shorten long URLs that never
      expire.
    </p>
  </div>
</div>
